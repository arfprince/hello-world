name: hello-world
on:
  push:

jobs:
  firstjob:
    runs-on: self-hosted
    steps:
      - id: step1
        run: echo "::set-output name=message::Hello from Step 1"
      - name: Use Step Output
        run: echo "${{ steps.step1.outputs.message }}"
  secondJob:
    runs-on: ubuntu-latest
    steps:
      - name: valo lage na
        run: echo "nomta boro udash"
      - name: Show commit SHA
        run: echo "Current commit SHA:" ${{ github.sha }}
      - name: Show branch SHA
        run: echo "Current commit SHA:" ${{ github.ref }}
